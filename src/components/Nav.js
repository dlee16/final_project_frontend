import React from 'react';
import { NavLink } from 'react-router-dom';

const Nav = () => {
    return (
        <div>
            <NavLink to ='/home'> Home</NavLink>
        </div>
    )
}

export default Nav